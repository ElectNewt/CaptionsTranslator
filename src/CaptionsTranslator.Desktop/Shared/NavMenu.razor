<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Captions Translator</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="configuration">
                <span class="oi oi-cog" aria-hidden="true"></span> Settings
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="youtube">
                <span class="oi oi-media-play" aria-hidden="true"></span> Youtube Video
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="youtubetranscription">
                <span class="oi oi-data-transfer-download" aria-hidden="true"></span> download YT .srt
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="filetranscription">
                <span class="oi oi-text" aria-hidden="true"></span> Translate .srt file
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

}